# Hand mouse control

Control the mouse with hand gestures through your webcam, using OpenCV + MediaPipe for hand tracking and PyAutoGUI to move the cursor.

## Running

```bash
pip install -r requirements.txt
python main.py
```

Move your hand in front of the webcam to move the cursor; gestures trigger the clicks.
